import React from 'react';
import SelectDoc from '../_components/SelectDoc';
function Workspace({ params }) {

  return (
    <div>
      <SelectDoc params={params} />
    </div>
  );
}

export default Workspace;
